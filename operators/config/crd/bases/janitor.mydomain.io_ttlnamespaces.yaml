---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: ttlnamespaces.janitor.mydomain.io
spec:
  group: janitor.mydomain.io
  names:
    kind: TTLNamespace
    listKind: TTLNamespaceList
    plural: ttlnamespaces
    singular: ttlnamespace
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TTLNamespace is the Schema for the ttlnamespaces API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TTLNamespaceSpec defines the desired state of TTLNamespace
            properties:
              targetNamespace:
                description: TargetNamespace is the name of the namespace to manage
                type: string
              ttl:
                description: Duration text format (e.g., "4h", "30m")
                type: string
            required:
            - targetNamespace
            - ttl
            type: object
          status:
            description: TTLNamespaceStatus defines the observed state of TTLNamespace
            properties:
              expiresAt:
                description: ExpiresAt tracks the exact deletion time
                format: date-time
                type: string
              phase:
                description: Phase tells us if it's "Active", "Notified", or "Expired"
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
